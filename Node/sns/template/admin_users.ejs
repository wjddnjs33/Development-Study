<html>
    <head>
        <meta charset="utf-8">
        <title>Admin Panel</title>
        <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
        <script>
            function x(i){
                    fetch("/update", {
                            method: 'POST',
                            headers: {'Content-Type':'application/json'},
                            body: JSON.stringify({
                                    id: i
                            }),
                    })
                    .then((res) => {
                        return res.json();
                    })
                    .then((data) => {
                        console.log(data);
                    });
             }
        </script>
    </head>
    <body>
        <hr>
        <h2 align='center'>Users Information</h2><hr>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>삭제</th>
                    <th>사용자 번호</th>
                    <th>이름</th>
                    <th>나이</th>
                    <th>아이디</th>
                    <th>비밀번호</th>
                    <th>관리자 권한</th>
                    <th>계정 활성화</th>
                    <th>계정 생성 시간</th>
                    <th>최근 로그인 시간</th>
                </tr>
            </thead>
            <tbody>
                <% for(i = 0; i < list.length; i++) { %>
                <tr>
                    <td><input type="button" onclick=x(<%=list[i].num %>); value="x"></td>
                    <td><%=list[i].num %></td>
                    <td><%=list[i].name %></td>
                    <td><%=list[i].age %></td>
                    <td><%=list[i].id %></td>
                    <td><%=list[i].pw %></td>
                    <td><%=list[i].admin %></td>
                    <td><%=list[i].email %></td>
                    <td><%=list[i].D %></td>
                    <td><%=list[i].Login_D %></td>
                </tr>
                <% } %>
            </tbody>
        </table>
        </div>
    </body>
</html>
